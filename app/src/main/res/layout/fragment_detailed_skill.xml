<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/related_tasks"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_below="@id/related_skill_layout"
                android:padding="@dimen/save_zone_margin" />
        </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="16dp"
        android:layout_marginBottom="16dp"
        android:src="@drawable/ic_mode_edit_black_24dp"
        app:layout_anchor="@id/related_tasks"
        app:layout_anchorGravity="bottom|right|end"
        app:rippleColor="@color/red"
        app:pressedTranslationZ="6dp"
        app:layout_behavior="com.levor.liferpgtasks.ScrollAwareFABBehavior"/>

</android.support.design.widget.CoordinatorLayout>