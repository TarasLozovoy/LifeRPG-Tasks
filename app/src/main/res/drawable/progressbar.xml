<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:angle="270"
                android:centerColor="@color/progress_bar_empty"
                android:centerY="0.75"
                android:endColor="@color/progress_bar_empty"
                android:startColor="@color/progress_bar_empty"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/progress_bar_center"
                    android:centerY="0.75"
                    android:endColor="@color/progress_bar_bottom"
                    android:startColor="@color/progress_bar_top"
                    />
            </shape>
        </clip>
    </item>
</layer-list>